# Local inputs

The V2X simulator generates scenarios procedurally; no dataset is required for
the template smoke tests. Place separately obtained GGUF weights under `models/`
or pass `--model-path`. Datasets, trajectories, model weights and private files
are intentionally excluded from the code release.
